name: Build iPadOS App

on:
  workflow_dispatch:

jobs:
  build-ipados:
    # Match branches that already build IPA successfully (not Blacksmith).
    runs-on: macos-15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.x"
          cache-dependency-path: modules/termix-tailscale/native/go.sum

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      # Userspace Tailscale (tsnet) C archive — must exist before prebuild/pods.
      - name: Build Termix Tailscale iOS archive
        run: |
          set -euo pipefail
          make -C modules/termix-tailscale/native ios
          test -f modules/termix-tailscale/ios/lib/libtermix_ts.a
          test -f modules/termix-tailscale/ios/lib/termix_ts.h
          # Ensure symbols we call from ObjC are present in the archive.
          nm -gU modules/termix-tailscale/ios/lib/libtermix_ts.a | grep -E 'TermixTS_(Configure|Up|StartForward|Close)'
          ls -lh modules/termix-tailscale/ios/lib/

      - name: Generate iOS project
        run: npx expo prebuild --platform ios --clean

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install CocoaPods dependencies
        run: |
          cd ios
          pod install

      - name: Get version
        id: version
        run: |
          VERSION=$(node -p "require('./app.json').expo.version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release directory
        run: mkdir -p release

      - name: Build unsigned IPA
        run: |
          cd ios

          # Build for generic iOS device (unsigned)
          xcodebuild -workspace Termix.xcworkspace \
            -scheme Termix \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath ../release/Termix.xcarchive \
            archive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE_SPECIFIER=""

      - name: Create unsigned IPA
        run: |
          cd release
          mkdir -p Payload
          cp -r Termix.xcarchive/Products/Applications/Termix.app Payload/
          zip -r termix_ios_${{ steps.version.outputs.version }}.ipa Payload
          rm -rf Payload

      - name: Upload iPadOS IPA
        uses: actions/upload-artifact@v4
        with:
          name: termix_ipados_${{ steps.version.outputs.version }}
          path: release/termix_ios_${{ steps.version.outputs.version }}.ipa
          retention-days: 30
          if-no-files-found: error
